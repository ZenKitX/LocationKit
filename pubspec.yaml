name: location_kit
description: A location service package for Flutter apps. Provides GPS positioning and location management.
version: 0.1.0
homepage: https://github.com/ZenKitX/LocationKit
repository: https://github.com/ZenKitX/LocationKit
issue_tracker: https://github.com/ZenKitX/LocationKit/issues

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
